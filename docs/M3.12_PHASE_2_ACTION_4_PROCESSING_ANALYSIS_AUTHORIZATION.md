# M3.12 Phase 2 Action 4 — Processing/Analysis Authorization and Evidence

## Decision and strict non-authority

**Status: accepted and executed 2026-08-05 UTC by the repository owner
(codeframe@gmail.com); isolated deterministic-double rehearsal only.** This
is the one-action `processing_analysis_authority` approval and evidence for
the same fixture reference and evidence record produced in
[Action 3 — intake-execution authorization](M3.12_PHASE_2_ACTION_3_INTAKE_EXECUTION_AUTHORIZATION.md).

The `M3RehearsalAdapter` bundles intake and processing/analysis into a single
deterministic-double outcome rather than exposing a separate processing
adapter; this preserves the contract's statement that it "has no ... process
... provider, or runtime adapter." The `processing` field of the evidence
record is this action's evidence.

## Processing/analysis evidence

| Field                        | Value                                                                                                                                                                                   |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Evidence identity            | `m3.12-rehearsal-evidence:sha256:f016430fa946c10d9c9e70929b7eb27481e0f4dbcd1a26cde57f5492617c85ee`                                                                                      |
| Processing-boundary category | `deterministic_double` — no worker, media tool, normalization, cue/fade, or provider call was made                                                                                      |
| Metadata mode exercised      | `deterministic_double` (see also the `failed` and `unavailable` modes proven safe-closed in [`tests/m3-rehearsal-adapter.test.ts`](../tests/m3-rehearsal-adapter.test.ts), lines 18–23) |
| Downstream planes            | Playout & Automation Runtime, Source Encoder Layer, and Listener-Facing Icecast Layer remained unavailable and were not contacted                                                       |

The adapter's `metadataMode: "failed"` and `metadataMode: "unavailable"`
paths were exercised separately in the committed test suite and return
`quarantined` / `unavailable` outcomes without throwing or invoking any
processing, media-tool, normalization, or provider adapter — proving the
processing boundary fails closed rather than substituting inferred data.

## What this authorization does not permit

It does not authorize a real FFmpeg, media-parsing, EBU R128, cue/fade, or
metadata-provider call. It does not authorize a change to any approved
M3.5/M3.6/M3.7 analysis contract. It authorizes only the recorded
deterministic-double evidence above.

## Next gate

The next step is the separate `lifecycle_review_authority` approval recorded
in [M3.12 Phase 2 Action 5 — review authorization](M3.12_PHASE_2_ACTION_5_REVIEW_AUTHORIZATION.md).
